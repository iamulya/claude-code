# Guardrails & Cost Control

Demonstrates YAAF's built-in guardrails for controlling agent costs, token limits, and safety boundaries.

## Run

```bash
GEMINI_API_KEY=... npm start
```

## What It Demonstrates

- Token budget enforcement and cost tracking
- Iteration limits (max tool call rounds)
- Output guardrails and content filtering
- Cost-aware agent configuration
